<p>Ciao <%= @resource.name %>,</p>

<% account_user = @resource&.account_users&.first %>

<% if account_user&.inviter.present? && @resource.unconfirmed_email.blank? %>
  <p><%= account_user.inviter.name %>, di <%= account_user.account.name %>, ti ha invitato a provare <%= global_config['BRAND_NAME'] || 'Chatwoot' %>.</p>
<% end %>

<% if @resource.confirmed? %>
  <p>Puoi accedere al tuo account <%= global_config['BRAND_NAME'] || 'Chatwoot' %> tramite il link qui sotto:</p>
<% else %>
  <% if account_user&.inviter.blank? %>
  <p>
    Benvenuto su <%= global_config['BRAND_NAME'] || 'Chatwoot' %>!  
    Abbiamo preparato per te una suite di strumenti potenti da esplorare.  
    Prima di iniziare, dobbiamo verificare il tuo indirizzo email per assicurarci che sia davvero tuo.
  </p>
  <% end %>
  <p>Fai clic sul link qui sotto per attivare il tuo account.</p>
<% end %>

<% if @resource.unconfirmed_email.present? %>
  <p><%= link_to 'Conferma il mio account', frontend_url('auth/confirmation', confirmation_token: @token) %></p>
<% elsif @resource.confirmed? %>
  <p><%= link_to 'Accedi al mio account', frontend_url('auth/sign_in') %></p>
<% elsif account_user&.inviter.present? %>
  <p><%= link_to 'Conferma il mio account', frontend_url('auth/password/edit', reset_password_token: @resource.send(:set_reset_password_token)) %></p>
<% else %>
  <p><%= link_to 'Conferma il mio account', frontend_url('auth/confirmation', confirmation_token: @token) %></p>
<% end %>